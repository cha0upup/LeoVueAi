# 2026-08-12

- 修改 git 邮箱：仓库局部 + 全局均改为 `chaodovvn@gmail.com`（用户名 `lichao` 不变）。
- 完成 LeoVueAi 仓库首次提交：`7392496 feat: init LeoVueAi frontend project`，536 个文件（已忽略 node_modules/、dist/）。
- 检查过 `.env.development` 仅含 `VITE_API_PORT=8082`，无敏感信息；注意 `.gitignore` 未忽略 `.env.development`，后续如在其中放入密钥需加进 gitignore。
